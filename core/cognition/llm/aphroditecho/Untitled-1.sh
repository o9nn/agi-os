# From /tmp/aphrodite-engine directory
cd /tmp/aphrodite-engine

# Create a new repository
gh repo create drzo/aphrodite-engine --public --source=. --push

# Or create as private
gh repo create drzo/aphrodite-engine-o --private --source=. --push
